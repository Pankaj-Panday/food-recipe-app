import config from "../config/config.js";

class UserService {
	// register user
	// login user
	// logout user
	// get current user
}

const userService = new UserService();

export default userService;
